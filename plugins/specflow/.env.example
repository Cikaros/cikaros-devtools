# specflow 环境变量（可选）
#
# ⚠ Codex CLI 的模型认证不读取本文件：
#   请使用 `codex login`（ChatGPT 账号）或导出 OPENAI_API_KEY 环境变量后再启动 codex。
#   v0.2.x 指引「把 OPENAI_API_KEY 写进 ~/.codex/.env」是错误的，已删除。
#
# 以下变量仅供 specflow 自身的模板渲染与 git hooks 使用，全部可选：

CODEX_AUTHOR_NAME="your-name"
CODEX_AUTHOR_EMAIL="your-email@example.com"
CODEX_PROFILE=relaxed
CODEX_LOG_LEVEL=info
